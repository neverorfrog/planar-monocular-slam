[project]
dependencies = ["rich"]
name = "pms_py"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project.scripts]
dataset = "pms_py.utils.dataset:main"
main = "pms_py.main:main"
